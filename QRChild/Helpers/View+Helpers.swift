//
//  View+Helpers.swift
//  QRChild
//
//  Created by Karapats on 20/06/ 15.
//  Copyright © 2018 Karapats. All rights reserved.
//

import Foundation
import UIKit

extension UIView {
    /*var snapshot: UIImage? {
        print("frame.size:\(frame.size)")
        print("frame.x:\(-frame.origin.x)")
        print("frame.y:\(-frame.origin.y)")
        UIGraphicsBeginImageContextWithOptions(CGSize(width: frame.size.width, height: frame.size.height), false, 0);
        drawHierarchy(in: CGRect(origin: CGPoint(x:frame.origin.x ,y:frame.origin.x),
                                 size: (superview?.bounds.size)!), afterScreenUpdates: true)
        let image:UIImage = UIGraphicsGetImageFromCurrentImageContext()!;
        UIGraphicsEndImageContext();
        return image
    }
    
    func snapshot(by size:CGRect) -> UIImage? {
        UIGraphicsBeginImageContextWithOptions(CGSize(width: 100, height: 100), false, 0);
        drawHierarchy(in: CGRect(origin: CGPoint(x:-50,y:-50),
                                 size: bounds.size), afterScreenUpdates: true)
        let image:UIImage = UIGraphicsGetImageFromCurrentImageContext()!;
        UIGraphicsEndImageContext();
        return image
    }*/
}
